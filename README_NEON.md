# ระบบบริหารจัดการสินค้าคงคลัง (Inventory Management System)
## 🚀 Powered by Neon PostgreSQL

ระบบจัดการสินค้าคงคลังที่พัฒนาด้วย PHP และ Neon PostgreSQL (Serverless Postgres) พร้อมฟังก์ชันครบครัน

## ✨ คุณสมบัติหลัก

### 1. ฐานข้อมูลอ้างอิง
- **1.1 จัดการข้อมูลลูกค้า** - เพิ่ม/แก้ไข/ลบข้อมูลลูกค้า
- **1.2 จัดการข้อมูลสินค้า** - เพิ่ม/แก้ไข/ลบข้อมูลสินค้า พร้อมระบบแจ้งเตือนสต็อกต่ำ

### 2. การทำงานประจำวัน
- **2.1 บันทึกการสั่งซื้อสินค้า** - สร้างคำสั่งซื้อแบบหลายรายการ มีการคำนวณยอดอัตโนมัติ
- **2.2 ประมวลผลข้อมูล** - อนุมัติคำสั่งซื้อ อัปเดตสถานะ และลดสต็อกอัตโนมัติ

### 3. รายงาน
- **3.1 รายงานกำหนดส่งสินค้า** - รายงานตามวันที่กำหนดส่ง พร้อมระบบแจ้งเตือนคำสั่งซื้อที่เลยกำหนด

### 4. ออกจากระบบ
- **4.1 ออกจากระบบ** - ออกจากระบบอย่างปลอดภัย

## 🛠️ ความต้องการของระบบ

- PHP 7.4 หรือสูงกว่า (รองรับ PDO และ pgsql extension)
- บัญชี Neon PostgreSQL (ฟรี!)
- Web Server หรือ PHP Built-in Server

## 📦 การติดตั้ง

### ขั้นตอนที่ 1: สร้างฐานข้อมูล Neon

1. ไปที่ https://console.neon.tech
2. สร้างบัญชีใหม่ (ฟรี)
3. สร้าง Project ใหม่
4. คัดลอก Connection String จากหน้า Dashboard

**ตัวอย่าง Connection String:**
```
postgresql://username:password@ep-project-name.region.aws.neon.tech/neondb?sslmode=require
```

### ขั้นตอนที่ 2: นำเข้าฐานข้อมูล

1. ไปที่ Neon Console > SQL Editor
2. คัดลอกเนื้อหาจากไฟล์ `database_neon.sql`
3. วางและรันใน SQL Editor

หรือใช้ `psql`:
```bash
psql "postgresql://username:password@ep-project-name.region.aws.neon.tech/neondb?sslmode=require" < database_neon.sql
```

### ขั้นตอนที่ 3: ตั้งค่าการเชื่อมต่อ

แก้ไขไฟล์ `config_neon.php`:

```php
define('DB_HOST', 'ep-project-name.region.aws.neon.tech');
define('DB_PORT', '5432');
define('DB_NAME', 'neondb');
define('DB_USER', 'your-username');
define('DB_PASS', 'your-password');
```

**วิธีหา Connection Details:**
1. ไปที่ Neon Console
2. เลือก Project ของคุณ
3. คลิกที่ "Connection Details"
4. คัดลอกข้อมูลแต่ละส่วนมาใส่ใน config

### ขั้นตอนที่ 4: ติดตั้ง PHP Extension

**Ubuntu/Debian:**
```bash
sudo apt-get install php-pgsql php-pdo
```

**macOS (Homebrew):**
```bash
brew install php
# pgsql extension มักจะมีมาพร้อมอยู่แล้ว
```

**Windows:**
แก้ไขไฟล์ `php.ini`:
```ini
extension=pdo_pgsql
extension=pgsql
```

### ขั้นตอนที่ 5: รันโปรเจค

**วิธีที่ 1: ใช้ PHP Built-in Server (แนะนำ)**
```bash
cd /path/to/project
php -S localhost:8000
```
เปิดเบราว์เซอร์: `http://localhost:8000/login_neon.php`

**วิธีที่ 2: ใช้ XAMPP/WAMP**
1. วางโฟลเดอร์โปรเจคใน `htdocs`
2. เปิดเบราว์เซอร์: `http://localhost/inventory/login_neon.php`

## 🔐 ข้อมูลเข้าสู่ระบบ

**Username**: admin  
**Password**: admin123

## 📁 โครงสร้างไฟล์

```
inventory/
├── config_neon.php           # การตั้งค่าฐานข้อมูล Neon
├── login_neon.php            # หน้าเข้าสู่ระบบ
├── logout_neon.php           # ออกจากระบบ
├── index.php            # หน้าหลัก/เมนูหลัก
├── customers_neon.php        # จัดการข้อมูลลูกค้า
├── products_neon.php         # จัดการข้อมูลสินค้า
├── orders_neon.php           # บันทึกการสั่งซื้อ
├── order_detail_neon.php     # รายละเอียดคำสั่งซื้อ
├── process_orders_neon.php   # ประมวลผลคำสั่งซื้อ
├── delivery_report_neon.php  # รายงานกำหนดส่งสินค้า
├── style.css                 # ไฟล์ CSS
├── database_neon.sql         # สคริปต์ SQL สำหรับ PostgreSQL
└── README_NEON.md            # คู่มือนี้
```

## 🎯 ความแตกต่างจากเวอร์ชัน MySQL

| ฟีเจอร์ | MySQL | Neon PostgreSQL |
|---------|-------|-----------------|
| ฐานข้อมูล | ต้องติดตั้ง Local | Serverless (Cloud) |
| Auto Increment | AUTO_INCREMENT | SERIAL |
| Connection | mysqli | PDO (pgsql) |
| SSL | ไม่บังคับ | ต้องใช้ (sslmode=require) |
| Scaling | Manual | Auto-scaling |
| ราคา | ฟรี (Local) | ฟรี 0.5GB + 3GB Transfer |

## 🌟 ข้อดีของ Neon PostgreSQL

✅ **Serverless** - ไม่ต้องติดตั้งหรือจัดการ database server  
✅ **Auto-scaling** - ปรับขนาดอัตโนมัติตามการใช้งาน  
✅ **Branching** - สร้าง branch ของ database ได้เหมือน Git  
✅ **Instant backups** - สำรองข้อมูลอัตโนมัติ  
✅ **ฟรี tier** - 0.5GB storage + 3GB data transfer  
✅ **PostgreSQL** - database ที่ทรงพลังและเป็นมาตรฐาน  

## 🔧 การแก้ไขปัญหา

### ไม่สามารถเชื่อมต่อ Neon ได้

1. **ตรวจสอบ Connection String:**
   - ตรวจสอบว่าคัดลอกถูกต้อง
   - ตรวจสอบว่ามี `?sslmode=require` ต่อท้าย

2. **ตรวจสอบ PHP Extension:**
   ```bash
   php -m | grep pgsql
   php -m | grep pdo_pgsql
   ```
   ถ้าไม่มี ให้ติดตั้งก่อน

3. **ตรวจสอบ Firewall:**
   - Neon ใช้พอร์ต 5432
   - ตรวจสอบว่า firewall ไม่บล็อก

### Error: "could not find driver"

ติดตั้ง PHP PostgreSQL extension:
```bash
# Ubuntu/Debian
sudo apt-get install php-pgsql

# macOS
brew install php

# Windows - แก้ไข php.ini
extension=pdo_pgsql
extension=pgsql
```

### Connection timeout

- ตรวจสอบว่าใช้ `sslmode=require` ใน connection string
- ตรวจสอบว่า Neon project ยังไม่ถูก suspend (ใช้งานอย่างน้อย 1 ครั้ง/7วัน)

## 📊 ข้อมูลเพิ่มเติม

### การสร้างผู้ใช้งานใหม่

รัน SQL ใน Neon Console:
```sql
INSERT INTO users (username, password, full_name) 
VALUES ('ชื่อผู้ใช้', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 'ชื่อเต็ม');
```

### การสร้าง Password Hash ใหม่

```php
<?php
echo password_hash('รหัสผ่านของคุณ', PASSWORD_DEFAULT);
?>
```

### การ Backup ข้อมูล

**จาก Neon Console:**
1. ไปที่ Settings > Backups
2. เลือก Point-in-time ที่ต้องการ
3. Restore หรือ Download

**ใช้ pg_dump:**
```bash
pg_dump "postgresql://user:pass@host/db?sslmode=require" > backup.sql
```

## 🚀 การ Deploy

### Deploy บน Hosting ที่รองรับ PHP + PostgreSQL

1. **Heroku:**
   - ใช้ได้ทันที (มี PostgreSQL extension)
   - ตั้งค่า connection จาก environment variables

2. **Railway.app:**
   - รองรับ PHP และ PostgreSQL
   - Auto-deploy จาก GitHub

3. **Vercel/Netlify:**
   - ใช้ Serverless Functions สำหรับ PHP
   - เชื่อมต่อกับ Neon ได้

## 📝 License

โปรเจคนี้สามารถใช้งานได้ฟรีเพื่อการศึกษาและพัฒนาต่อยอด

---

## 🆘 ต้องการความช่วยเหลือ?

- **Neon Docs:** https://neon.tech/docs
- **Neon Discord:** https://discord.gg/92vNTzKDGp
- **PostgreSQL Docs:** https://www.postgresql.org/docs/

---

**เวอร์ชัน**: 1.0 (Neon Edition)  
**วันที่สร้าง**: 2026-02-09  
**Database**: Neon PostgreSQL (Serverless)  
**ผู้พัฒนา**: Inventory Management System  
